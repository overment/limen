# PASS

The narrow Limen 1.0 plan fits the dependable-core boundary authorized by Adam in this conversation. It bounds existing notification attempts, removes bot-specific configuration, separates transport from receiver evidence, repairs startup and supervision, and subtracts duplicated prompt policy. It does not require a workflow engine, silent model fallback, automatic coordinator ownership, blanket review gates, or a new control plane. Running-owner recovery (F049) restores the existing supervisor for an already authorized job; it neither starts another worker nor adopts an unwatched job into this conversation. Its filesystem claim prevents competing supervisors, not human decisions. The overlapping wake, finish, and hosted-process seams will run sequentially in isolated worktrees, with this coordinator as the sole landing and board writer. This is a plan gate, not a release or runtime-proof claim.

## Exact implementation spawn order

1. Wake errors count toward the two-attempt ceiling (F090).
2. Finish webhook names become bot-agnostic (F092).
3. Finish inspection separates transport from completed bot turn (F091).
4. Spawn refuses an unusable route (F081).
5. Hosted runtime starts its own agent (F048).
6. Running jobs regain truthful supervision (F049), after F048.
7. Prompt policy has one prose home (F087).

One implementation worker per ticket; no shared-checkout parallel writers. Hosted workers use `--provider openai-codex --model gpt-6-astra --thinking high`. This coordinator is running `openai-codex/gpt-6-astra:xhigh`. Adam retains slice review; no independent reviewer is authorized by the existing board or wake ticket. Implementation, repairs, evidence inspection, and earned Git landings remain this coordinator's responsibility.

## Evidence and limits

- Read the required vision, board, styleguide, vision HTML, handoff, notes, and checklist; also read all seven must-land tickets and the existing unusable-route repair note.
- The gate task's `spec/styleguide.md` does not exist. Read the project-owned `.agents/limen/styleguide.md` identified by the shop manual instead.
- `spec/build.md` matches the seven active and six planned folders at baseline `59ce943`; `limen jobs` reports no running jobs. Unrelated historical work is not adopted.
- The checklist's unanswered boundary decision is superseded by Adam's authorization in this conversation. No human-owned vision rewrite is needed.
- First external finish proof still requires the authorized Alice project on Mac and correlated Johnny/Tony completed-turn evidence. This VPS, HTTP acceptance, and existing presentation checks cannot substitute for that proof; it remains an external evidence dependency, not permission to invent a receiver API.
- The unusable-route repair note correctly rejects authentication-only proof. If the native route interface would require a paid generated probe or new routing machinery, expose that cost/interface question rather than broaden the ticket or claim acceptance.
- Quota preserves work without substitution. Prompt-policy consolidation stays prose-only and must retain the owner's review, proof, and quota choices on resume.
- No product tests or live wake/finish proof ran during Phase 0.

## Deferred beyond narrow 1.0

Background field guide (F050), Claude perspective (F074), closing overview (F075), history retention (F086), remote-seat rollout (F013), and GitHub doorbell (F014). No expanded research/quality/picture automation, integration rewrite, or reopening the landed explicit-model work (F088/F089). Tag/release remains Adam's decision after the cut is proven.
