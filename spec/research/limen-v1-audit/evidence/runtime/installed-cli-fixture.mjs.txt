import net from 'node:net';import {spawn,spawnSync} from 'node:child_process';import {rm,mkdir,writeFile,readFile} from 'node:fs/promises';const dir='/tmp/limen-runtime-audit.s79R5Y/socket-fixture';await mkdir(dir,{recursive:true});const path=dir+'/herdr.sock';await rm(path,{force:true});let protocol=22;const requests=[];const server=net.createServer(socket=>{let b='';socket.on('data',data=>{b+=data;let i;while((i=b.indexOf('\n'))>=0){const r=JSON.parse(b.slice(0,i));b=b.slice(i+1);requests.push(r);socket.end(JSON.stringify({id:r.id,result:r.method==='ping'?{type:'pong',version:'0.8.2',protocol}:{type:'ok'}})+'\n');}})});await new Promise(r=>server.listen(path,r));const env={...process.env};for(const k of Object.keys(env))if(/^(HERDR_|LIMEN_|PI_)/.test(k))delete env[k];Object.assign(env,{HERDR_SOCKET_PATH:path,HERDR_CONFIG_PATH:dir+'/config.toml',HERDR_ENV:'1'});async function cli(args){return new Promise(resolve=>{const p=spawn('/opt/homebrew/bin/herdr',args,{env,stdio:['ignore','pipe','pipe']});let stdout='',stderr='';p.stdout.on('data',b=>stdout+=b);p.stderr.on('data',b=>stderr+=b);const t=setTimeout(()=>p.kill('SIGKILL'),10000);p.on('close',status=>{clearTimeout(t);resolve({status,stdout,stderr})});});}await mkdir(dir+'/has spaces',{recursive:true});const log=dir+'/has spaces/log';await writeFile(log,'fixture output\n');const args=['pane','run','w1:p1','tail','-n','+1',log];const first=await cli(args);const expected=/client protocol (\d+)/.exec(first.stderr);let result=first;if(expected){protocol=Number(expected[1]);result=await cli(args);}const cmd=requests.find(r=>r.method==='pane.send_input')?.params.text;const shell=cmd?spawnSync('/bin/sh',['-c',cmd],{cwd:dir,encoding:'utf8'}):undefined;console.log(JSON.stringify({first,result,protocol,requests,shell: shell&&{status:shell.status,stdout:shell.stdout,stderr:shell.stderr}}));await new Promise(r=>server.close(r));await rm(path,{force:true});