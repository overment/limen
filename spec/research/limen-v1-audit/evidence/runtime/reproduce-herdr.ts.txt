import {spawn,spawnSync} from 'node:child_process';import {mkdir,writeFile,readFile,readdir,chmod,rm}from'node:fs/promises';import{dirname,join}from'node:path';import{scratchRepo,limen,onlyJobId,waitForState,LIMEN,git}from'./limen/test/scratch.ts';import{openJobPlace}from'./limen/src/herdr.ts';
const delay=(n:number)=>new Promise(r=>setTimeout(r,n));
function environment(s:any,more:any={}){const e={...process.env};for(const k of Object.keys(e))if(/^(LIMEN_|HERDR_|PI_)/.test(k))delete e[k];return{...e,PATH:`${s.fakeBin}:${process.env.PATH}`,LIMEN_HERDR:'0',LIMEN_HUNK:'0',LIMEN_HOME:dirname(s.root),...more};}
async function until(path:string,match:RegExp){const d=Date.now()+20000;while(Date.now()<d){const v=await readFile(path,'utf8').catch(()=>'');if(match.test(v))return v;await delay(30)}throw Error('deadline '+path);}
async function moved(){const s=await scratchRepo();let p:any;const previous=process.env.LIMEN_HERDR;try{const j=join(s.root,'job'),bin=join(s.fakeBin,'herdr'),calls=join(s.root,'calls');await mkdir(join(j,'herdr'),{recursive:true});for(const[k,v]of Object.entries({state:'running',label:'moved worker','started-at':new Date().toISOString(),hosted:'1',log:'',worktree:s.root,branch:'main',base:git(s.root,'rev-parse','HEAD')}))await writeFile(join(j,k),v+'\n');for(const[k,v]of Object.entries({workspace:'w1',tab:'w1:t1',pane:'w1:p1',agent:'w1:p1',mode:'hosted'}))await writeFile(join(j,'herdr',k),v+'\n');await writeFile(bin,`#!${process.execPath}\nconst fs=require('node:fs'),a=process.argv.slice(2);fs.appendFileSync(${JSON.stringify(calls)},JSON.stringify(a)+'\\n');const ok=result=>console.log(JSON.stringify({result}));if(a[0]==='agent'&&a[1]==='get'){if(a[2]==='w1:p1'){console.error(JSON.stringify({error:{code:'agent_not_found',message:'old target missing'}}));process.exit(1)}ok({agent:{pane_id:'w2:p2',tab_id:'w2:t2',workspace_id:'w2',name:'limen-fixture',agent_status:'working'}})}else if(a[0]==='agent'&&a[1]==='list')ok({agents:[{pane_id:'w2:p2',tab_id:'w2:t2',workspace_id:'w2',name:'limen-fixture',agent_status:'working'}]});else if(a[0]==='tab'&&a[1]==='get')ok({tab:{tab_id:a[2],pane_count:1,label:'unrelated occupant'}});else ok({});`);await chmod(bin,0o755);p=spawn(process.execPath,[LIMEN],{cwd:s.root,env:environment(s,{LIMEN_INTERNAL_HOSTED:'1',LIMEN_JOB_DIR:j,LIMEN_HOSTED_TARGET:'w1:p1',LIMEN_AGENT_NAME:'limen-fixture',LIMEN_HERDR:bin}),stdio:'ignore'});await until(join(j,'log'),/relocated/);process.env.LIMEN_HERDR=bin;const opened=await openJobPlace({jobDir:j,cwd:s.root,running:true});await writeFile(join(j,'session-ended'),'yes\n');await until(join(j,'state'),/done/);await until(calls,/close/);console.log('MOVED_PLACE',JSON.stringify({opened,pane:await readFile(join(j,'herdr/pane'),'utf8'),tab:await readFile(join(j,'herdr/tab'),'utf8'),workspace:await readFile(join(j,'herdr/workspace'),'utf8'),calls:(await readFile(calls,'utf8')).trim().split('\n').map(JSON.parse)}));}finally{if(p?.pid)try{process.kill(p.pid,'SIGKILL')}catch{}if(previous===undefined)delete process.env.LIMEN_HERDR;else process.env.LIMEN_HERDR=previous;await s.cleanup();}}
async function updates(){const s=await scratchRepo(`#!${process.execPath}\nconsole.log(JSON.stringify({type:'agent_start'}));for(let i=0;i<50;i++)console.log(JSON.stringify({type:'message_update',assistantMessageEvent:{type:'text_delta',delta:'a'}}));console.log(JSON.stringify({type:'message_end',message:{role:'assistant',content:[{type:'text',text:'done'}]}}));\n`);try{limen(s,'init');const counts=join(s.root,'git-count');await writeFile(join(s.fakeBin,'git'),`#!${process.execPath}\nconst cp=require('node:child_process'),fs=require('node:fs'),a=process.argv.slice(2);if(a[0]==='status'&&a[1]==='--porcelain')fs.appendFileSync(${JSON.stringify(counts)},'status\\n');const r=cp.spawnSync('/usr/bin/git',a,{encoding:'utf8'});process.stdout.write(r.stdout||'');process.stderr.write(r.stderr||'');process.exit(r.status??1);`);await chmod(join(s.fakeBin,'git'),0o755);const id=onlyJobId(limen(s,'spawn','delta burst').stdout);await waitForState(s.root,id,'done',60000);console.log('DELTA_GIT_COST',JSON.stringify({messageUpdates:50,gitStatusCalls:(await readFile(counts,'utf8')).trim().split('\n').length}));}finally{await s.cleanup();}}
await moved();await updates();
