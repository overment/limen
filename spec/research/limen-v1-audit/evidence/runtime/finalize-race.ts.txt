import{spawn}from'node:child_process';import{mkdir,readFile,readdir,writeFile,chmod}from'node:fs/promises';import{dirname,join}from'node:path';import{scratchRepo,git}from'./limen/test/scratch.ts';const s=await scratchRepo();try{const j=join(s.root,'job'),b=join(s.root,'barrier');await mkdir(j);await mkdir(b);for(const[k,v]of Object.entries({state:'running',base:git(s.root,'rev-parse','HEAD'),branch:'main',worktree:s.root,log:''}))await writeFile(join(j,k),v+'\n');await writeFile(join(s.fakeBin,'git'),`#!${process.execPath}\nconst f=require('node:fs'),cp=require('node:child_process'),a=process.argv.slice(2);if(a[0]==='log'){f.writeFileSync(${JSON.stringify(b)}+'/'+process.ppid,'1');let end=Date.now()+10000;while(f.readdirSync(${JSON.stringify(b)}).length<2&&Date.now()<end)Atomics.wait(new Int32Array(new SharedArrayBuffer(4)),0,0,10)}const r=cp.spawnSync('/usr/bin/git',a,{encoding:'utf8'});process.stdout.write(r.stdout||'');process.stderr.write(r.stderr||'');process.exit(r.status??1);`);await chmod(join(s.fakeBin,'git'),0o755);const env={...process.env};for(const k of Object.keys(env))if(/^(LIMEN_|HERDR_|PI_)/.test(k))delete env[k];Object.assign(env,{PATH:`${s.fakeBin}:${process.env.PATH}`,LIMEN_HERDR:'0'});const source=`import {finalizeJob} from '${new URL('./limen/src/wrapper.ts',import.meta.url).href}';await finalizeJob(process.argv[1],process.argv[2],process.argv[2]+' contender');`;const results=await Promise.all(['done','failed'].map(state=>new Promise(r=>{const p=spawn(process.execPath,['--input-type=module','--eval',source,j,state],{env,stdio:['ignore','pipe','pipe']});let stderr='';p.stderr.on('data',b=>stderr+=b);p.on('close',status=>r({state,status,stderr}));})));console.log('FINALIZER_RACE',JSON.stringify({results,state:await readFile(join(j,'state'),'utf8'),log:await readFile(join(j,'log'),'utf8')}));}finally{await s.cleanup();}