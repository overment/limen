# Finish-webhook reliability — diagnose recurring failures (Adam 2026-09-10)

## Authorize
Figure out **why limen → Grok Bot finish webhooks keep failing**, and fix what limen/ops can fix. Astra xhigh coordinator + workers. Do **not** print secrets, tokens, or full env files.

Tony shepherds; model: `--provider openai-codex --model gpt-6-astra --thinking xhigh`

## Recent signal
- Grok Bot automation **Limen coordinator finish ping** reported failure: **`[unauthenticated] Error`** (2026-09-10 ~10:30 Warsaw) while a Mac limen job was finishing.
- Earlier F702/F702b work: fan-out is on limen `main`; Alice app/API historically had **no** `.limen/finish-webhook.env` project opt-in; home `~/.overment/tony-finish-webhook.env` is manual-only; HTTP 200 ≠ owner wake.
- Evidence/history: `tmp/evidence/F702b/1d77c03b/`, `docs/finish-webhooks.md`, `alice-finish-reliability-notes.md`

## Goals
1. Reproduce / classify failure modes: unauthenticated, timeout, missing opt-in, wrong Auth header, multi-target partial failure, helper exit 127, wake-not-observed.
2. Name whether the break is **sender (limen helper)**, **project opt-in**, **Grok Bot webhook receiver**, or **ops config**.
3. Deliver a **fix or bounded repair plan** limen can own (code + docs + optional project opt-in recipe). Do not invent bot endpoints; use existing Tony finish-ping contract `{job,status,branch}`.
4. Prove with focused tests + one controlled ping that does not leak secrets; record HTTP status separately from wake observation.

## Deliverables
Under `spec/research/finish-webhook-reliability/2026-09-10/`:
- `findings.md` — root causes with evidence
- `fix-or-plan.md` — what landed vs what needs Adam (e.g. fresh webhook key)
- Evidence under `tmp/evidence/finish-webhook-failures/`

## Finish
`~/.overment/tony-finish-ping.sh limen-webhook-reliability done <branch>`
(If that helper is itself broken, document the catch-22 and use an alternate safe signal.)

## Out of scope
Alice product UI; printing credentials; Claire/Rose/Tom.
