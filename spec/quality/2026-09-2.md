# Quality findings — 2026-09-2

Stretch: the ten PROVEN features ending at Pi/OMP engine selection (`0f74748`). Prior September findings that already landed (dirty-file count, prompt inventories, static footer, private sweep helper) are not repeated.

## Small unification slice · Stop teaching Pi as the only job binary

The profile table already selects Pi or OMP (`src/engine.ts`), and the shop's engine paragraph states that. The spawn-mode table three paragraphs earlier still says hosted runs interactive `pi` and detached runs background `pi` (`templates/agents.md`). The hosted-guarantees paragraph then says the runtime "starts `pi` there." README copies the engine paragraph verbatim, then still tells the operator to continue "a finished Pi job." A successful detached run records `pi exited 0` even when the profile was OMP (`src/wrapper.ts`). A hosted supervisor that cannot classify the pane logs that it "still runs pi" (`src/supervisor.ts`). One slice: make those operator and log surfaces follow the selected profile id. Leave coordinator launch as Pi; that path was never dual-engine.

## Proposed ticket line · Hosted pane liveness still looks for a process named pi

After a hosted start, relocation treats a pane as present when Herdr lists the agent or when the foreground process is named `pi` or `node` (`src/herdr.ts` `locateHostedAgent`). The profile table launches `--kind omp` and an `omp` binary is not in that name list. A hosted OMP pane that Herdr cannot classify can look missing, which is the path that finalizes "hosted agent ended." This is not a re-review of the engine landing; live hosted OMP was already unproved. The follow-up is to recognize the selected engine's process the same way the profile table already recognizes its kind.

## Proposed ticket line · Continue still publishes an empty live job directory

Overlapping starts now write `started-at` and `worktree` in a hidden `.publishing-<id>` directory and rename it into `.limen/jobs/` so prune never sees an empty in-flight path (`src/commands/spawn.ts`). Continue still `mkdir`s the live job path, then writes markers (`src/commands/continue.ts`). Prune still deletes a job directory with no `state` and no parseable `started-at` immediately (`src/commands/prune.ts`). Spawn itself prunes finished worktrees during start, so a continue that has created the directory but not yet written `started-at` is the same empty-cabinet window the spawn publication rename closed. Give continue the same hidden-directory publish; do not add a new job phase.

## Small subtraction slice · Drop the zero-changed-files recovery heuristic

The running dirty-file count is gone because it was neither complete nor stable. Coordinator recovery still says a live worker "with tool calls and zero changed files" gets a steer that names the file and the first edit (`templates/agents.md`). That clause treats the dropped jobs-line number as the first recovery signal. Pulse, log, commits, and an inspected worktree remain. Delete the clause; do not restore the counter.

## Drop-candidate · The living picture still describes last month's board

`spec/picture.md` claims the board's NOW is still hosted spawn returning in seconds (F048, folder supposedly still active), that next is a dead-supervisor recovery (F049) then the remote seat (F013) then the GitHub doorbell, that no quality findings file exists, and that the vision still keeps the coordinator on a laptop until the seat is proven. Vision and TRACK already speak in the past tense for spawn, reaper, and seat; F013 is proven; `spec/quality/` exists. Two files therefore claim where work stands, and the picture is the one that lies. Do not patch NOW/NEXT in a slice. Drop the file until a picture pass replaces it, or start that pass because engine selection and seat vs window moved the shape.

## Coordinator disposition

- Hosted engine recognition is planned as `F728-hosted-engine-liveness`; the continuation publication window is planned as `F729-continuation-publication`. Both claims match the current source, but no deterministic failure reproduction or live OMP proof was run for this findings pass.
- Engine wording and recovery-evidence clarification share `F730-job-guidance-matches-engines`. A directly inspected worktree can still show zero changes; the problem is ambiguous guidance, not proof that steering by observed lack of progress is wrong.
- The stale picture finding stays with the parked background field-guide proposal (F050). The board explicitly parks that work, so this disposition neither starts a picture job nor deletes the artifact.
- The report changed only Markdown. The coordinator read its complete diff and source seams; independent review was not warranted. Runtime fixes remain unstarted, and the Pi/OMP landing is not reopened.
- Filing checks: typecheck and `git diff --check` passed; structure passed 4/4 from a clean staged-tree export. The structure run in the coordinator checkout failed its unique-TypeScript-basename assertion because retained untracked evidence contains copied source trees; those evidence files were preserved.
