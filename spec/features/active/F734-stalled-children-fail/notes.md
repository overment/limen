# Stalled-tool seam

- Hosted OMP and Pi session JSONL records carry an assistant `toolCall` and a matching `toolResult` by `toolCallId`; a pending tool has no result yet. The hosted extension supplies live `tool_execution_start` / `tool_execution_end` boundaries because the session file need not be flushed during the pending call. Local OMP sample: job `2026-09-26-f734-stalled-child-fails-instead-8c3b9901`, first call/result 09:06:35.203/09:06:35.209 UTC. Local Pi sample: job `2026-09-25-f733-diagnose-secure-github-seat-6d92993f`, first call/result 08:50:54.482/08:50:54.498 UTC.
- Alice's named `ae-2-routes` 08:11–08:28 UTC cargo gap was reported in the ticket but not located in `/home/overment/alice/.limen/jobs`, `/home/overment/.pi/agent/sessions`, or `/home/overment/.omp/agent/sessions` on the `alice` SSH host. Those paths had 170 JSONL files; none contained both `T08:11:` and `T08:28:` with `cargo test`. Obtain the exact Herdr session path if the next reviewer needs to compare the historical gap with the new diagnosis.
- Herdr `pane process-info --pane <target>` reports foreground PID and argv, including the job's `--session-dir`; the hosted hook records its own engine PID. The supervisor is not that engine's parent. The detached wrapper is its own process-group leader and owns the engine child. Darwin birth identity comes from `proc_pidinfo`; Linux birth identity is `/proc/<pid>/stat` start ticks. Cumulative `ps` CPU TIME over the verified engine tree, stable child ancestry and no output progress must hold throughout `LIMEN_TOOL_STALL_MS` (three minutes by default).
- Manual smoke: hosted OMP and detached Pi each failed after the 1.2-second configured window with an absent sleeping child. An active CPU-consuming tool and a silent model-thought observation did not confirm a stall. Formal native tests, typecheck and formatter are intentionally left for the coordinator's post-branch check, per handoff.
