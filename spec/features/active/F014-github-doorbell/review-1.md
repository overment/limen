Coordinator inspection of candidate `1ffb7c4` (not an independent reviewer verdict).

Blocking: the isolated poller treats `.limen/github/claims/*.json` and `.limen/jobs/*` as authoritative for App PR replies, but `github connect` makes the claims directory group-writable by `overment`, the same Unix identity as every hosted worker. A hosted worker can create a synthetic claim with `receipt: "prompt accepted"`, make a matching hosted-looking job record, and cause `reconcile` to post start/terminal comments with the App's installation token without an exact command or write-authorized commenter. The poller must keep its accepted claims/cursor in a poller-owned location outside the worker-writable checkout; worker-readable handoff data may be a copy, but must not confer posting authority. Add a test proving a forged checkout claim cannot cause an App receipt.

Blocking: `reviewGithubClaim` calls `spawnCommand` without engine/provider/model/thinking, despite the board's explicit launch policy. The project coordinator must select and pass the board's model flags explicitly for a hosted review, without a default silently making the choice. Keep the command path constrained to the registered Herdr coordinator and pinned base/head.

Nonblocking: the candidate deleted all of `test/structure.test.ts`, including the dependency-free and TypeScript-basename guards, to accommodate new source. Retain useful boundary assertions or explain the deliberate subtraction rather than dropping them to make room. `templates/reviewer.md` still says every review is detached; distinguish the doorbell-hosted exception.

Checks inspected: candidate `npm run check` passed TypeScript and Biome, five doorbell tests passed, 462/468 Node tests passed. On main, the four affected legacy test files reproduce five failures (continue, template history, three macOS prune paths). The candidate's isolated `test/init-command.test.ts` rerun passed 9/9; its full-suite failure was not reproduced. No App/key, Linux UID separation, Herdr delivery, or live PR was tested. Do not call this operationally green yet.
