The package implementation merged at `75332a5`: an isolated GitHub App poller, exact authorized PR command, private accepted claims and cursor, Herdr coordinator prompt, pinned hosted review, and start/terminal receipts. `docs/remote.md` is the Alice operator guide. The checkout is deliberately still active: no real App key, Linux service identity, Herdr prompt, test PR receipt, or two-seat route was exercised. Do not claim operational proof from mock API tests.

Focused TypeScript, Biome and 20 doorbell/structure/inheritance/init tests passed at the candidate. A full native run after repair timed out after 15 minutes; before the timeout it reproduced the five failures already seen on main (one continue diagnostic, one template-history test before repair, and three macOS nested-path assertions; the template-history and OMP-init tests passed in the latest partial run). The earlier complete candidate lane was 465/471 before the template history correction. Re-run the native lane on the installed seat only if it can finish; compare failures with the main baseline rather than declaring all green.

Read-only inspection of the Alice VPS showed `/home/overment/alice` at `alice-v6`, origin `https://github.com/iceener/alice.git`, with `.limen/jobs/` present. The `overment` account still has sudo and NOPASSWD:ALL, Node 24 exists only in worker-owned NVM, and `setfacl` is absent. These must be corrected by separate root administration before `limen github connect`; no Alice files were edited. The App and install are external authority Adam/Tony must provision. Easytools remains disconnected.
