The isolated GitHub App poller and pinned review path landed first (`75332a5`); the mention front door, warm-coordinator gate, retry, and seat doctor landed in `a745289`, `f74a6b3`, and the Alice repair through `912f9e0`. The worker account has no sudo; the poller alone owns the PEM and private state. `/opt/limen`, Node 24, Herdr 0.9.1, the narrow ensure/deliver sudo policy, and the timer are root-managed. `limen github doctor` is green in `/home/overment/alice`, and `github ensure` reaches the existing Herdr coordinator `w1K:p2`.

On open `iceener/alice` PR #10, the `iceener` account's `@limen` comment `5830141928` received one pending notice while the original sudo policy lacked `github ensure`. After the policy repair, the same claim reached the coordinator and started hosted review `2026-09-25-pr-10-review-5830141928-56683924` at pinned head `a21d51a` and base `ff526ff`. The job ended `done`; the App posted one start and one terminal reply, with no duplicate after another poll. The review's PASS result also reports a merge conflict and red frontend tests; neither the job nor the App approved, merged, pushed, or edited Alice.

This feature stays active for a live second-seat ownership/routing proof. A Mac SSH alias reaches Alice's live Herdr agent, but attaching with `herdr --remote alice` from a fresh Mac terminal was not exercised inside this already hosted coordinator pane; no pin change was needed for the PR flow.
