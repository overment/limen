# Living architecture picture: evidence map

This is a proposal study, not an implemented watcher. No runtime, board, ticket, outcome, vision, or role prompt is changed.

## Baseline

Limen snapshot: `8bffbc0e6707c75e81971957a50d823bd6a44cdc`.

- `README.md`: one human conversation, isolated short jobs, Git/file evidence; clean job exit is not feature completion.
- `spec/vision.md`: picture runs after a shape changes, quality judges stretches of landed work, research requires authorized spend. None owns merges or the board.
- `templates/picture.md`: today's hard output boundary is **only** `spec/picture.md`; a named shape change is required. A multi-file atlas would be an explicit contract change, not an existing capability.
- `templates/quality.md`: immutable findings under `spec/quality/`, assessed against vision/styleguide; no code changes or ticket opening.
- `templates/researcher.md`: named-source answers, filed by the coordinator. Not an automatic dependency scanner.
- `templates/agents.md`: picture scheduling currently depends on coordinator judgment after merge. Quality is after ten proven landings or human request. `--role` loads a prompt; `--detached` separately selects background execution.
- `spec/picture.md`: mixes a useful enduring explanation with an old board snapshot. It names uncertainty (tests-only routing; missing quality reports), but has no explicit inspected revision and does not offer module drill-down.
- `spec/build.md`: current read-only board differs from the picture's NEXT list. The proposal ticket exists under active but is not in NOW. Do not reconcile either in this job.

## Proposal seam

Separate the durable map from short-lived evidence: a whole-application overview, stable responsibility addresses, a few end-to-end journeys, and dated/revision-bound condition claims. Background observation must not become authority to merge, open tickets, or mark a feature proven. Stale is a visible condition, not an excuse to regenerate the world.

## Alice snapshot and development loop

Alice snapshot: `fd67336e94bd3a13eb86341c04de265cef5020a2` at `/Users/overment/playground/alice-app/alice`. Read-only; untracked `bun.lock`, two feature paths and `spec/linear.md.off` are excluded from claims. No native app, credentials, workspace content or `w98:t1` interaction is authorized.

- `spec/vision.md`: the product is a headless runtime; desktop/chat are clients. Eight composable primitives, four semantic durable authorities, one contract/dispatch, disposable projections. The map must not mistake source directories, conceptual primitives, and durable stores for equivalent nodes.
- `.agents/limen/styleguide.md`: one authority per state family; generated over hand-synced; evidence closes the loop; module exemplars already form a cheap starting map. It is normative, not a source census.
- `spec/build.md`: peer coordinators serialize shared writers and land at one primary checkout; isolated workers produce focused evidence, Adam dogfoods, and unrun native acceptance remains separate from merged code. Its visible recent window ends around F602 while Git and done folders include later landings through F652 and audit filings. This is evidence of a stale board view, not permission for the picture to fix it.
- `spec/quality/2026-09.md`: five concrete subtraction/unification findings over clipboard/Settings landings F428–F453 at `85d7cd898`; not a current app-wide health verdict.
- `spec/features/done/2026-09/F641-vision-styleguide-alignment-audit/notes.md`: selective audit at `c33fcea56…`, not this snapshot. It identifies a direct contradiction between one-reply vision wording and chronological narration; five follow-ups already exist (F657–F661). Picture should attach and age this finding, not open duplicate tickets.
- `spec/features/done/2026-09/F643-agent-owned-alice-lab-instances/architecture.md`: isolated lab proposal distinguishes deterministic proof from future authorized live dogfood. Being in done means the specification landed, not that two isolated live labs work.
- `spec/features/done/2026-09/F644-desktop-and-web-dual-surface-map/source-map.md`: static survey separates planned browser access from existing rejecting HTTP transport. A future transport must not appear as a solid implemented edge because vision wants it.

## Implementation seams inspected

- `src/commands/spawn.ts::resolvePreamble`: project role overlay, then package prompt. Mode is selected separately; role alone does not detach.
- `src/commands/spawn.ts::spawnCommand`: creates isolated branch/worktree and records base, branch, role, task, state, subscribers. Completion notification is job-scoped, not a landing event. The worker output is still a candidate branch.
- No landing-watcher command is listed in the README. Existing `limen watch` subscribes to jobs; do not reuse its name to imply Git observation already exists.

## Pinned source checks and recent delivery evidence

- `crates/alice/src/runtime.rs`: separately constructs work database, Workspace, ValueHandle and JournalStore, then wires injected Code/HTTP/Secrets into RuntimeTools. This supports the authority view, not a blanket claim that every mutation obeys it.
- `src/lib/ipc/client.ts` and `src-tauri/src/commands.rs`: typed request/subscription envelope; runtime requests call Runtime, while `account.*` routes to the host account owner. Do not flatten this into “every operation is kernel dispatch.”
- `src/lib/ipc/http.ts`: request and subscribe both explicitly reject as unimplemented at the pinned revision. The map must show a missing browser route, not an implemented future edge.
- `src/lib/thread/project.ts`: chronological narration insertion and stable per-beat reply slots directly corroborate the recent audit's vision disagreement.
- `crates/alice-ai/{Cargo.toml,src/lib.rs}`: concrete protocols depend on `alice`; current modules include HTTP/MCP/OAuth/search.
- **Disconfirming dependency check:** `crates/alice-code/Cargo.toml` has no `alice` dependency. `src-tauri/src/code_runner.rs::HostCodeRunner` implements the runtime CodeRunner interface and adapts to the independent process runner. The initial assumption that both concrete crates directly depend on the kernel was false and was removed from the wireframe. A shared responsibility area does not imply identical dependency direction.
- One-Integration delivery: `spec/features/done/2026-09/F580-one-integration-chat-and-images/outcome.md` distinguishes focused proof, missing isolated-native proof, and Adam's subsequent OpenRouter confirmation. These are separate evidence labels.
- Nested-reply delivery: `spec/features/done/2026-09/F595-nested-child-root-never-settles/outcome.md` records a pre-repair falsifier and 55 passing protocol/fold tests at `81981f46924b70dc731bdd2613e39231442e9b19`; native acceptance unrun. Referenced primary-checkout summary exists; its contents were not audited here.
- Recent navigation delivery: `spec/features/done/2026-09/F652-resources-history-gallery-capabilities/notes.md` records focused/native evidence and broader failed lanes separately. This folder has **no outcome.md**; use the actual notes and Git, not a fabricated outcome. Its focused receipt exists; this study neither opened its frames nor reran Alice.

Pinned source copies and SHA-256 manifest are retained under `/Users/overment/.overment/limen/tmp/evidence/F050-study/sources/`. The 27 captured Alice files matched the primary checkout when captured; untracked files were not used. This preserves revision-bound evidence despite concurrent peer landings.

## Presentation seam

Working direction: an application **field guide**, not an activity dashboard. Stable places, labelled responsibility edges, and journeys make the whole shape legible; intent and evidence are alternate readings of the same places, not separate graphs. A selective audit and a completed design ticket supply particularly useful negative examples: old evidence is not current verification, and a landed proposal is not shipped capability.

The UI skill's remote `mcp__uidotsh__uidotsh_fetch` tool is unavailable in this job. The example will use local, dependency-free HTML/Markdown and inspected browser frames, not claim compliance with unfetched remote guidance.
