# Outcome

The optional `/speak` command shipped, then was removed at the owner's request. Pi no longer registers playback, even when a `speak` CLI is on PATH. Removal landed in `d45fed2`. The ticket in this folder is the original shipped scope, not current product.
