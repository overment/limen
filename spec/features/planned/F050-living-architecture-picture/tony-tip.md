# Tony: a background field guide for the whole application

The proposal is committed as `a3f5d100f86457d164b39ac8784e619bb7dd1949` on `limen/2026-09-08-living-architecture-picture-a9d9c072`.

Start with the directly openable Alice example: `/Users/overment/.overment/limen/tmp/evidence/F050-study/guide/example.html`.

The design lives at `spec/features/active/F050-living-architecture-picture/proposal.md`; repository evidence is in sibling `notes.md`, the text example in `example.md`, and actual verification/limits in `checks.md`.

The recommendation is a background picture worker maintaining stable responsibility places and a few user journeys, with Structure / Intent / Evidence lenses over the same map.

A cheap opt-in seat timer observes one integration ref, coalesces landings and starts ordinary detached jobs; there is no perpetual model conversation, Alice app dependency, or automatic quality/research/repair fan-out.

Markdown is the semantic artifact; an optional shared renderer publishes a disposable local HTML view from committed picture branches without merging into the product branch.

Today's picture role still requires a named shape and writes one file only; the multi-file guide and landed-range entry mode are explicit proposed contract changes, not existing behavior.

Alice's actual loop motivates the evidence labels: focused green landings, Adam's dogfood, incomplete native checks and landed designs coexist; the guide must not turn them into one green status.

The wireframe separates the rejecting HTTP transport from a landed browser design, puts the chronological-narration/vision disagreement beside the affected surface, and distinguishes provider-adapter dependencies from the host-bridged Code runner.

The source/browser check passed twice, including once at the clean candidate; negative controls rejected fictional HTTP success and an invented Code dependency, and all six candidate frames were opened.

The full Limen check ran once: TypeScript/Biome passed; 156 passing and three failing test lines appeared before the 600-second timeout. It is incomplete, not green; causes remain untriaged in this documentation job.

Reviewer artifacts, source snapshots/hashes, the replay script and raw logs survive outside the worktree at `/Users/overment/.overment/limen/tmp/evidence/F050-study/`.

No runtime, role, board, ticket, outcome, vision or styleguide changes were made; Alice and `w98:t1` were untouched. No child workers or independent reviewer were spawned.

Next: try the cold-navigation tasks in the proposal and decide whether automatic local publication, with optional reviewed documentation merges, is the right boundary; that is the recommended option.

Remaining work is implementation only if this proposal is accepted; the human navigation trial and all watcher/race/freshness checks are unrun.

Routing: no Tony route was supplied and no named Tony appeared in the inspected Herdr agent list. The coordinator must forward this tip; it was not sent to an unidentified pane or the creator-app tab.
