# Outcome

## Result

Landed. Completion wakes carry bounded `commits` and `result`. Hosted `done` is session end or vanished agent (`hostedTerminalReason`), not Herdr unseen-idle. `limen stop` does not fabricate `result`. Live-trial of the 90s false-done is in `trial.md`.

Merged `437a4dc` on human instruction without independent review (review `923ee450` stopped). Coordinator rebased over `18e28b8` and F018; `tsc` and biome clean; suite 98/100 then the stop-bound flake passed on rerun.

## Date

2026-08-18

## References

- `437a4dc`
- Finish job `2026-08-18-f017-finish-grok-07575ba7`
- Trial `spec/features/done/2026-08/F017-completion-handoff/trial.md`
