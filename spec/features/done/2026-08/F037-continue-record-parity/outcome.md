# Outcome

`limen continue` is a spawn sibling: copies parent `repo`, finalizes a failed launch, runs `LIMEN_PREFLIGHT=auth` before writing a record, and is hosted in Herdr unless `--detached`. `limen jobs <id>` names `parent`.

Landed at `62aad12`. Hosted review PASS (`review-1.md`) of candidate `62aad1235f533586de7f78697796054876a925a1`.
