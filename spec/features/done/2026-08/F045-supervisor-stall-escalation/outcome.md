# Outcome

A stalled hosted worker rings and stamps its pane from the supervisor. Live prove 2026-08-25: job `2026-08-25-f045-stall-prove-5638a53d`, unwatched. After one tool call and ~60s idle: advisory `idle 1m after 1 tool calls, session still open`; pane `w1H:p17` display-agent `⚠ stalled 1m` with idle/done/blocked state labels. Job stayed `running`. Merged `e71dcef`; review PASS of `9139a20`.
