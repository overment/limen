# Outcome

## Result

Landed as guidance only. One paragraph in `templates/agents.md` beside “This is craft, not a gate” names when the coordinator should edit inline versus spawn. Worker template needed no change. Projects with an `AGENTS.md` overlay do not inherit this automatically.

Coordinator-written at `da52ae5`. Docs-only; `npm run check` green. No independent reviewer: complete local diff, cheap to revert.

## Date

2026-08-18

## References

- `da52ae5`
