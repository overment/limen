# Outcome

`limen spawn --review` in Herdr opens an interactive Pi tab, same as implementation spawn. `--detached` still means a log-tail reviewer. The review worktree stays a detached checkout.

Merged `914bc4d`. Live-proven via F030's hosted implement/review loop.
