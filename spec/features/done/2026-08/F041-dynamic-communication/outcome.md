# Outcome

Limen now chooses the shortest clear response for the question and stakes instead of following a fixed format matrix. The package register restores enough current context for an owner who missed recent work, separates evidence from judgment, and gives durable prose the same standalone-reader standard.

The injection mechanism did not change. `hook/communication.ts` still appends the effective register to the system prompt at the start of each user turn. README, CONTRIBUTING, comments, tests, and the F008 board summary now describe that timing accurately.

Implementation commit `0fa6d06`. Fresh review PASS after six isolated model probes. TypeScript, Biome 2.5.8, and communication-hook tests passed in a clean environment. The full serial suite passed 150/156; the remaining process-control failures reproduce outside the F041 files and were accepted as non-blocking by review.
