# Pi and OMP share one engine profile

Pi and OMP jobs now launch through one profile table, one wrapper, and the unchanged stream parser, selected by `--engine pi|omp` or `LIMEN_ENGINE`. The coordinator fast-forwarded main to implementation commit `0f7474883dfd74a8448bf2fefde225cbedd724b4`; continuation preserves the parent engine and rejects a conflicting selection before creating a child job. Coordinator checks passed 72/72 across focused spawn, continuation, profile, stream, structure, hosted, wrapper-finalization, and template-history coverage; typecheck and Biome also passed. Pi and OMP credentials remain separate, and live hosted OMP and live OMP continuation were not proved. The full worker check hit its 600-second cap, so this landing claims focused proof rather than a single full-suite pass; commands and retained output are listed in `checks.md`.
