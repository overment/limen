# Outcome

A completion or idle wake waits minutes before falling back, and only to a session that has spawned or watched a job. A wake that dies on a provider error is retried. The text opens with the job’s label and task, and the instruction comes last. A routed wake tells a helper not to spawn, stop, or steer unless the human asks.

Landed `5ac67d3`. Review PASS of `99356b3`. Merge kept the failed-turn `errored` advisory kind.
