# Outcome

A project overlay that still matches an older package template is named stale at `limen init` and in the per-turn drift note, with the date it matches and the date the package last changed. `--drop-leftovers` still deletes only copies of the current package.

Landed `6228859`. Review PASS of `0ae402b`. Regenerated `templates/.history` after the guidance merge so the shipped hashes match HEAD.
