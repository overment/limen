Hosted spawn and continuation return before agent readiness while the detached supervisor owns startup and terminal finalization, including after the caller is killed. The existing runtime already met this contract; the landing adds regressions and a retained acceptance map rather than a runtime rewrite. Candidate `d93b411` landed in merge commit `c6d4d6c271ca5648789423ae75f0e13c02d57792` after coordinator inspection under Adam's delivery instruction, without an independent reviewer. The worker's focused lane passed 87 checks, and the coordinator's three killed-caller/continuation/tab-refusal checks passed with CLI return observed in under one second in the isolated fixtures. The single full native lane passed TypeScript/Biome and 381 tests but cancelled the registry-lock test at 60 seconds; no live provider or installed-Herdr compatibility claim is made. Recovering a supervisor that dies later remains the separate running-owner change (F049).
