# Outcome

The shop manual names the seat/window split and points at the remote operator doc. Spawn only on the seat; a laptop is a window. Linux gaps are headed: process containment is macOS-shaped, hosted tabs already dropped it, stop is best-effort. Landed `d33e334`. Docs only; no host-mismatch advisory and no Linux containment port. Structure tests passed 4/4; native was split after a 600s cap, remaining files 100/100.
