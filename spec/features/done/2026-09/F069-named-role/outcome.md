# Outcome

Spawn `--role <name>` loads that prompt file and opens a Herdr space named after the project plus that name with an `s`. Worker and reviewer stay the default and `--review`; a missing preamble or combining `--role` with `--review` plants no job. Continue inherits the parent's role unless `--review` is passed.

Landed `3ec3f21`. Review PASS of `3ec3f21`. Source-line cap moved 3348 → 3353.

The researcher, judge, quality, and picture prompts and their rituals are still to ship.
