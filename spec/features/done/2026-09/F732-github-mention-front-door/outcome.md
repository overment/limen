Authorized `@limen` and `/limen` comments on an open PR now reach the repository's warm Herdr coordinator with bounded PR context, pinned refs, and the comment's request. A missing coordinator leaves one visible pending notice; the same claim retries safely, and the App posts one hosted-job start and one terminal reply rather than treating prompt acceptance as completion. The implementation merged at `a745289` and the live Alice handoff repairs landed through `912f9e0`. On `iceener/alice` PR #10, comment `5830141928` followed that pending-to-hosted-review path; the review ended and a further poll left exactly one notice, one start, and one terminal reply. Two project roots pointing to one coordinator were exercised in the focused integration test; a live second repository and second seat remain outside this proof.
