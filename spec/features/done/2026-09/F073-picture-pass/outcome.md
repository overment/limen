# Outcome

A picture pass is a named role: after a merge, spawn `--role picture --detached` only when the handoff can name the shape that moved. It rewrites `spec/picture.md` and nothing else. Prompt wording, tests, and ordinary slices are not a picture.

Landed `8259cb1` (merge of `adf6542`). Review PASS of `adf6542`.
