# Outcome

A review's first line is `PASS <sha>` or `FAIL <sha>`. Blocking is only a proven break of an acceptance bullet; a plausible race, a lint bypass, or a missing check is a note. Reviewers install from the lockfile instead of failing a worktree with no `node_modules`.

Landed `3ecafa0`. Review PASS of `f556f14`.
