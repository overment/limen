# Outcome

`limen spawn --task-file` writes the task bytes untouched. A missing `git` is retried once and then an absolute path; a failed spawn after mkdir removes the job dir. `LIMEN_PREPARE` or `--prepare` runs in the worktree before Pi and is logged, not fatal. `limen jobs` names a dir with no state as orphan; `limen prune` deletes it.

Landed `6b7cdb4`. Review PASS of `17fee8d`.
