# Outcome

Limen starts jobs only with Pi. `--engine claude` and `--role advisor` fail before any job exists. Landed `14c99d9`. Focused spawn/stream/structure tests passed; full native 453/453. Context walkthrough no longer teaches a Claude engine. Reload Pi for the new shop manual.
