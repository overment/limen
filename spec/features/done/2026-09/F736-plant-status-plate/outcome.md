# Outcome

`limen status` now shows RUNNING job truth across Herdr spaces, unmerged candidate branches awaiting the owner, and relevant working coordinator tabs without equating a Herdr `done` badge to a landed branch. If global agent discovery fails, recent recorded origin tabs appear as incomplete evidence; missing Git stays unconfirmed. Merged as `e1a9d2f` with batched Git classification and fallback correction `fd267a5`; four real-Git status tests passed. The live plant plate showed a running worker, two working coordinator tabs, and unmerged historical branches in under five seconds.
