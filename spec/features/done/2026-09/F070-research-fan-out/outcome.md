# Outcome

When you ask a question that needs more than one opinion, spawn at least two researcher jobs on different models, then one judge that names where they diverged. Reports file as `report-N.md` and `judgment.md` in the feature folder or `spec/research/`. Research never merges. Named sources only; no search extension.

Landed `8233fb1` (merge of `c02f8f8`). Review PASS of `c02f8f8`.
