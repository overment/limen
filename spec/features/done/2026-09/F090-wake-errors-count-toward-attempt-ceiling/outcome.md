# Outcome

An unchanged job notification stops after two unsuccessful automatic attempts, including provider error, aborted, and rejected injection. The pending claim stays inspectable; recovery is deliberate. Landed `a14640f`. Focused wake suites passed 64/64; an installed Pi failing-provider replay recorded exactly two wakes and no third after reload; full native passed 422/422.
