# Outcome

A tab title says what the work changes, in plain words, with the feature number last instead of first. Job directory names still lead with the number because the id hoists it from anywhere in the label, so the hosted agent name and `limen close FNNN` keep working whichever way the words are ordered. Spawn warns, without refusing, when a label is only a feature number or when a live job already carries the same one.

Landed `01ab5dc`. Merged on coordinator inspection with the full check suite green; no independent review.

Not proven: how wide a description survives the sidebar before it truncates. The next job spawned with a forty-character label settles that by eye.
