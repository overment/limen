# Outcome

Moment-of-use style and vision reminders now name the active project files and quote up to eight of their headings. They no longer recite Limen’s TypeScript or one-human rules in other projects. Writes and edits outside `spec/` count as code unless the path is Markdown, covering Rust, Svelte, CSS, and other languages.

Landed `91ea670`. Review PASS of `b929056`. Communication tests passed 22/22 after merge; TypeScript and Biome passed.
