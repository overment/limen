# Outcome

`collectSweep` is file-private. Nothing outside the wake hook called it. Sweep behavior is unchanged. Landed `482715d`. Focused wake-hook and wake-sweep tests passed 64/64; typecheck clean.
