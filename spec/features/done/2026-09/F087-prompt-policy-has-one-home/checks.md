# Prompt-policy consolidation: landing evidence

Candidate `6e4039810c28ddb4d268f721b52abd7533d9b3be` landed as merge `ec65dc266410f7bbe46366a7997f8ae2dd0db21a`. Coordinator inspection covered the complete diff, terminal job record, session commands, retained logs and package contents. Adam's no-independent-review instruction was retained; no Adam candidate-review verdict is claimed.

Evidence root: `/home/overment/limen/tmp/evidence/F087-prompt-policy-8229a46a/`.

- `candidate.diff` matches the inspected Git diff byte-for-byte. The candidate checkout and its recorded pre/post-native status were clean.
- The shop manual holds complete handoff shape and explicit owner/board precedence. The communication register loses only its duplicate handoff instruction. Runtime files and all role preambles are unchanged.
- The removed structure tests assert prose wording, not delivered behavior. Eighteen such tests disappear; architecture, history, pulse and job-file contracts remain. The retained parent-test probe fails specifically because the handoff instruction moved out of the register (`baseline-probe.log`).
- Worker focused checks passed 71/71 (`focused.log`). Existing history generation passed 3/3, adding one current hash per changed template without removing prior entries (`history-generation.log`).
- Coordinator ran all structure/history checks and the five existing named-role spawn cases: 12/12 passed (`coordinator-checks.log`). Independent inspection of the retained npm tarball matched all nine templates and histories against candidate Git blobs, verified current hashes and retained every parent history entry.
- Full native lane ran once at the clean candidate: TypeScript/Biome passed; 368 passed, zero assertion failures, one cancelled, exit 1 (`native.log`). The registry dead-lock test at `test/sweep-command.test.ts:50` exceeded 60000ms. Its cause remains unproven; it was neither repaired nor rerun.

No generated route probe, receiver verification, private configuration migration or live finish ping was attempted. Neither the parked wake candidate nor the preserved route counterexample changed. This change adds no runtime model selection, board parser or review gate.
