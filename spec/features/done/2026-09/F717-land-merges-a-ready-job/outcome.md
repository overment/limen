# Outcome

`limen land <id>` merges a done job onto the current branch with one ordinary `git merge`. `--yes` skips the TTY prompt for plant use. It refuses a running job, empty commits, a dirty target, and `--onto` that is not already checked out. No job files or review gates. Landed `659c05b`. Focused land tests passed. Full native: TypeScript and Biome passed, 405/406; the miss is the existing wake-sweep timing bound. Adam reviews; no independent reviewer.
