# Outcome

Shop manual, speech register, vision, and styleguide ride the system prompt once per call. The per-turn note is a short cue. A write under `spec/`, an edit of code, or a spawn/merge gets the matching rule on the tool result.

Landed `ea537ba`. Review PASS of `3043809`.
