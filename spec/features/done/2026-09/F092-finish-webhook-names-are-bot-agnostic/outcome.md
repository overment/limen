Finish delivery now uses only `LIMEN_FINISH_WEBHOOK_*` configuration in the helper, automatic caller, setup docs, and packaged guidance; retired keys have no aliases. Legacy-only selected configuration fails before sending, while single-target and multi-target delivery retain their existing payload and selection rules. Candidate `d0eb175` landed in merge commit `a76e0ae9b564d540d710189bb3815242d50c71bd` after coordinator inspection under Adam's delivery instruction, without an independent reviewer. The worker's 76 focused checks and the coordinator's typecheck plus 79 helper/lifecycle/template-history checks passed; the full native lane passed TypeScript/Biome and 371 tests but cancelled one registry-lock test at its 60-second timeout. Private configuration was not inspected or migrated, and no live endpoint or receiver-turn proof was performed. Operators must migrate single-target files and launcher overrides; multi-target keys are unchanged, and separate bot-turn receipts remain the next slice.
