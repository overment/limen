# Outcome

A quality pass is a named role: spawn `--role quality --detached` loads its prompt, and the shop manual starts that pass after ten proven landings or when you ask. It writes one findings file under `spec/quality/` and does not rewrite the tree. Findings become tickets or one small slice; the pass is not a merge gate.

Landed `cb61dec`. Review PASS of `cb61dec`.
