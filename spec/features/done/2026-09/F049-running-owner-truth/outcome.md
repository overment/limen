A running hosted job whose supervisor dies now regains watch-only supervision when its existing agent is concretely alive, while confirmed ownerless work fails and uncertain Herdr evidence waits. Recovery never starts another agent or changes coordinator subscriptions, and competing attempts produce one owning supervisor. The implementation and coordinator's test-only correction landed as merge `6662bac4afad29c647f2d76ecf7e61247a892aeb` after coordinator inspection under Adam's delivery instruction, without an independent reviewer. Worker focused checks passed 103; the corrected candidate passed coordinator typecheck and all 50 wake/reaper/recovery checks. The original full native lane passed TypeScript/Biome and 385 tests but had the subsequently corrected wake-fixture failure and a registry-lock timeout; no second full run or real-seat/macOS recovery proof was performed. New CLI invocations use the recovery code, while already-open coordinators need `/reload` to load the updated hook dependency.
