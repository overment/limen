# Outcome

Pi workers and continuations now default to `openai-codex/gpt-6-astra:high` instead of inheriting the global Pi model. Trusted coordinator startup in this repository uses Codex/xhigh, and the README documents explicit Herdr peer flags and optional worker exports; Adam's global Grok default remains unchanged. Implementation landed directly on main in `aa5382e`, with independent review omitted at Adam's request. Focused routing checks, typecheck, and scoped formatting passed; the full native lane remains incomplete because of existing formatting/source-budget failures and a ten-minute test timeout, detailed in `notes.md`. The linked install uses the runtime change immediately; existing coordinators need `/reload` for the revised launch guidance, not for worker model selection.
