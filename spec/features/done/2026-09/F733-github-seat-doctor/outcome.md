The seat now has a checked-in root setup, systemd units, and `limen github doctor` with actionable ownership, credential, timer, registry, binding, and live-agent checks. The implementation merged at `f74a6b3`, with Alice-discovered Herdr envelope, Git trust, sudo traversal, and narrow ensure-policy repairs landed through `912f9e0`. On Alice, the setup installed root-owned Node 24, Herdr 0.9.1, and `/opt/limen`; the worker has no sudo or App-key access, and the poller has a private key and state. `github doctor` passed after the timer was enabled, `github ensure` reached the existing coordinator, and the isolated service delivered and reconciled the live PR request. The second-repository path was exercised in integration tests, not against another live App installation.
