The inherited shop manual now opens by saying the coordinator is a hosted Pi tab in Herdr, not a limen job, and that `limen spawn` only starts workers, reviewers, and advisors. A short table maps Tony starting a coordinator to that Pi tab, and the coordinator managing workers to that same session running spawn. README install agrees in one sentence. Landed `5305eeb`. `limen init` still does not copy the shop manual into the project; coordinators inherit `templates/agents.md` unless the project overlays `AGENTS.md`. Reload the coordinator to pick up the new package text.
