# Outcome

The per-turn project-context note names `spec/vision.md`, `spec/build.md`, and `.agents/limen/styleguide.md` and when each must be read. The file bodies stay on disk. A missing board still produces its advisory; wakes still skip the note.

Landed `67c0c70`. Coordinator-checked: the communication-hook suite passed, including pointer lines present, bodies absent, and a file planted between turns.
