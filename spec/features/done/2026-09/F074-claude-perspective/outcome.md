# Outcome

The coordinator can buy a Claude perspective as a detached job: `--role advisor --engine claude`. It never merges, never edits the board, and never writes code. This shipped earlier in `96832c3`; the planned folder is now filed. No new runtime in this wave.
