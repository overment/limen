# Outcome

Package workers are told not to edit the board, ticket status, or outcome files; to read only the ticket, findings, and named file before the first edit; to run the discriminating check first; and to end with `finish`. A slice that would go wider than the handoff commits what is useful, writes the question, and finishes.

Landed `85a26c2`. Review PASS of `ab6331d`. This repository’s overlay `.agents/limen/worker.md` is unchanged and still wins over the package template.
