OMP workers now see plant skills authored in either portable `.agents/skills` or legacy `.pi/skills` layouts without per-skill hand links. Native OMP skill names retain precedence, and continued jobs refresh their legacy skill view on launch. Merged the implementation and collision repair in `f40ac5b`; the first independent review caught the precedence defect before landing. Real OMP workers in an isolated plant read legacy, portable, and native skills in detached and hosted modes, and a continuation discovered a new skill; Alice's three previously missing skills were read by a fresh OMP session without changes to Alice. Focused tests, typecheck, and Biome passed; the full suite had unrelated macOS path and timing failures and was not claimed green.
