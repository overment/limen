FAIL 255404302ad8cf334ed03bcbee318151f2371980

- **PROVEN — blocking:** A legacy skill replaces a native OMP skill with the same name. `prepareSkillConfig` skips duplicates in `.agents/skills` but not `.omp/skills` (`src/engine.ts:94`), then adds the legacy view through `skills.customDirectories` (`src/engine.ts:122`). In an independent live probe of this candidate, `.omp/skills/native/SKILL.md` contained `# Native right`, `.pi/skills/native.md` contained `# Legacy wrong`, and actual OMP `read skill://native` returned `# Legacy wrong`. This breaks the ticket’s native-precedence acceptance bullet. Exclude names supplied by native OMP directories from the legacy view.

**Checks run:** `git rev-parse HEAD` matched the candidate; the tree was clean. `npm ci --ignore-scripts` completed. `npm run typecheck` passed; `npx biome check` passed for the six checked files. Focused Node suites recorded 105 passes, 3 failures, and 1 cancellation out of 109 tests. The new skill-view and selected-branch tests passed. Failures were a detached continuation timeout, a missing-branch error assertion, a hosted job that stayed running, and a concurrent-job announcement assertion; none establishes another skill-discovery defect, and the lane was not rerun. The suites use fake OMP, so they do not prove real discovery. The live probe establishes the collision; Alice’s named-skill acceptance was not independently checked here.
