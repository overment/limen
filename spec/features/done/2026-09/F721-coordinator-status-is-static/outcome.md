# Outcome

The coordinator footer no longer animates a spinner. Status still updates when a sweep produces a new body; clearing still removes it. Landed `ab70649`. Focused wake tests passed after one timing flake on an unrelated 20 ms sweep budget; full native 456/456. Reload Pi for the static footer.
